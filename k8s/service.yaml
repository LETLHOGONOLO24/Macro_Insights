apiVersion: v1
kind: Service
metadata:
  name: macro-insights-service
spec:
  type: NodePort
  selector:
    app: macro-insights-api
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30007
